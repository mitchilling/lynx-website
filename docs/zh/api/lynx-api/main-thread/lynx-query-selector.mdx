---
title: lynx.querySelector()
api: lynx-api/lynx/querySelector
---

import { APISummary, APITable } from '@lynx';

# lynx: querySelector(selector: string)

<APISummary />

在页面的子元件中查找第一个满足指定选择器的元件。返回一个 [`MainThread.Element`](../main-thread/main-thread-element)。如果没有满足要求的元件，返回 `null`。支持的选择器列表见 [selector](../selector-query/selector-query-select#selector)。

## 语法

```ts
const element = lynx.querySelector(selector);
```

### 参数

#### selector

指定选择器，支持的选择器列表见 [selector](../selector-query/selector-query-select#selector)。

### 返回值

[`MainThread.Element`](../main-thread/main-thread-element) 或 `null`。

## 兼容性

<APITable />
