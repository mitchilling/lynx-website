---
api: css/properties/letter-spacing
---

import { APISummary, APITable, Go } from '@lynx';

# letter-spacing

<APISummary />

## 介绍

`letter-spacing` 属性用于设置文本字符的间距表现。

## 使用示例

<Go
  example="css-api"
  defaultFile="src/letter-spacing/App.tsx"
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/doc/css/letter-spacing.jpg"
  entry="src/letter-spacing"
  defaultEntryFile="dist/letter-spacing.lynx.bundle"
/>

## 语法

```css
/* <length> values */
letter-spacing: 0.3em;
letter-spacing: 3px;
letter-spacing: 0.3px;
```

### 取值

- [`<length>`](/api/css/data-type/length.mdx)
  指定文字间的间距以替代默认间距。可以是负值。

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>empty value</>}
  appliesTo={<>text</>}
  inherited="yes"
  animatable="no"
/>

## 形式语法

```
<length>
```

## 与 Web 的区别

- 不支持 `normal` 关键字。只支持 `<length>`。

## 兼容性

<APITable />
