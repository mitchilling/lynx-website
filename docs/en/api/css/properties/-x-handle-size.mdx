---
api: css/properties/-x-handle-size
---

import { APISummary, APITable, Go } from '@lynx';

# -x-handle-size

<APISummary />

## Introduction

The `-x-handle-size` property specifies the size of the floating marker when copying text, only valid in the selection pseudo-element.

## Examples

<Go
  example="css-api"
  defaultFile="src/-x-handle-size/App.tsx"
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/doc/css/-x-handle-size.jpg"
  defaultEntryFile="dist/-x-handle-size.lynx.bundle"
  entry="src/-x-handle-size"
/>

## Syntax

```css
::selection {
  -x-handle-size: 5px;
}
```

### Values

- [`<length>`](/api/css/data-type/length.mdx)<br/>
  Sets the size of the floating marker

## Formal definition

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>0</>}
  appliesTo={<>text</>}
  inherited="no"
  animatable="no"
/>

## Formal syntax

```
-x-handle-size = <length>
```

## Differences from web

- This is a Lynx-specific property.

## Notes

- On Android, it corresponds to the size of the entire floating marker, while on iOS, it corresponds to the diameter of the floating marker's circular ball

## Compatibility

<APITable />
