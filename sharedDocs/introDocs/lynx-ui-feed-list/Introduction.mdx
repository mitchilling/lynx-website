import { Go } from '@lynx-ui/index';
import {
  feedListBasicDemoMeta,
  feedListDirectRefreshDemoMeta,
  feedListFooterDemoMeta,
} from './demoMeta.ts';

## Basic Usage

Use FeedList when the page needs both virtualized list rendering and feed
behaviors such as pull-to-refresh or load-more. Keep each row as a direct
`list-item` child with a stable `item-key`, and provide `listId` plus `listType`
so the underlying list can manage identity and layout.

<Go
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/lynx-ui/lynx-ui-cover-feed-list.webm"
  example="lynx-ui-feed-list"
  defaultEntryName="FeedListBasic"
  entry="Basic"
  defaultFile="Basic/index.tsx"
  highlight={feedListBasicDemoMeta.highlight}
  schema="{{{url}}}?fullscreen=true&bar_color=0d0d0d&bg_color=0d0d0d"
  webPreviewMode="auto"
/>

## How to manage footer status

Use `loadMoreFooter` for the loading state and `noMoreDataFooter` when the feed
has reached the end. Switch between them through `changeHasMoreStatus`; this
footer state is independent from refresh state and list diff results.

<Go
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/lynx-ui/v0/lynx-ui-cover-feed-list-footer.webm"
  example="lynx-ui-feed-list"
  defaultEntryName="FeedListBasic"
  entry="Basic"
  defaultFile="Basic/index.tsx"
  highlight={feedListFooterDemoMeta.highlight}
  schema="{{{url}}}?fullscreen=true&bar_color=0d0d0d&bg_color=0d0d0d"
  webPreviewMode="auto"
/>

## How to trigger refresh directly

Use `startRefresh` when refresh should be driven by a command instead of a pull
gesture, such as a retry button or external filter change. After the data request
finishes, call `finishRefresh` to release the refresh header.

<Go
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/lynx-ui/lynx-ui-cover-feed-list_refresh.webm"
  example="lynx-ui-feed-list"
  defaultEntryName="FeedListRefresh"
  entry="Refresh"
  defaultFile="Refresh/index.tsx"
  highlight={feedListDirectRefreshDemoMeta.highlight}
  schema="{{{url}}}?fullscreen=true&bar_color=0d0d0d&bg_color=0d0d0d"
  webPreviewMode="auto"
/>
