import { Go } from '@lynx-ui/index';
import { inputBasicDemoMeta, inputKeyboardAwareDemoMeta } from './demoMeta.ts';

Input component that supports controlled, uncontrolled, and keyboard avoidance capabilities. Use `<Input>` for single-line input and `<TextArea>` for multi-line input.

## Basic Usage

Use `<Input>` to render a single-line input field, and `<TextArea>` to render a multi-line text area.

<Go
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/lynx-ui/v0/lynx-ui-cover-input-basic.webm"
  example="lynx-ui-input"
  defaultEntryName="InputBasic"
  entry="Basic"
  defaultFile="Basic/index.tsx"
  highlight={inputBasicDemoMeta.highlight}
  schema="{{{url}}}?fullscreen=true&bar_color=0d0d0d&bg_color=0d0d0d"
  webPreviewMode="auto"
/>

## Avoiding Keyboard Overlap

Prevent the on-screen keyboard from obscuring the input field by automatically adjusting the view when the keyboard appears

<Go
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/lynx-ui/v0/lynx-ui-cover-input-keyboard-aware.webm"
  example="lynx-ui-input"
  defaultEntryName="InputKeyboardAwareView"
  entry="KeyboardAwareView"
  defaultFile="KeyboardAwareView/index.tsx"
  highlight={inputKeyboardAwareDemoMeta.highlight}
  schema="{{{url}}}?fullscreen=true&bar_color=0d0d0d&bg_color=0d0d0d"
  webPreviewMode="auto"
/>

## Structure

```jsx
<KeyboardAwareRoot>
  <view className='container'>
    <KeyboardAwareResponder>
      <KeyboardAwareTrigger>
          <Input
            style={{
              width: '100%',
              padding: '10px',
              backgroundColor: 'yellow',
            }}
          />
      </KeyboardAwareTrigger>
      <KeyboardAwareTrigger>
          <TextArea
            style={{
              width: '100%',
            }}
            placeholder="textareaId:'textarea2'"
          />
      </KeyboardAwareTrigger>
    </KeyboardAwareResponder>
  </view>
<KeyboardAwareRoot>
```

## API
