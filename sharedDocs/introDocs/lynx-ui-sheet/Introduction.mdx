import { Go } from '@lynx-ui/index';
import {
  sheetAutoHeightDemoMeta,
  sheetBasicDemoMeta,
  sheetControlledDemoMeta,
  sheetDirectionalDemoMeta,
  sheetImperativeDemoMeta,
} from './demoMeta.ts';

Sheet is a sliding surface for contextual actions, detail panels, and transient
workflows. It supports bottom sheets, top sheets, side drawers, snap points,
dragging, backdrop dismissal, and both controlled and imperative usage.

## Basic Usage

Start with `SheetRoot`, `SheetView`, `SheetBackdrop`, and `SheetContent`. Put
the visual surface on `SheetContent`, then use the backdrop and drag gestures for
natural dismissal.

<Go
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/lynx-ui/lynx-ui-cover-sheet.webm"
  example="lynx-ui-sheet"
  defaultEntryName="SheetBasic"
  entry="Basic"
  defaultFile="Basic/index.tsx"
  highlight={sheetBasicDemoMeta.highlight}
  schema="{{{url}}}?fullscreen=true&bar_color=0d0d0d&bg_color=0d0d0d"
  webPreviewMode="auto"
/>

## Auto Height

Use the `fit` snap point when the panel height should follow its content. This
works well for menus, short forms, and content whose height is not known ahead of
time.

<Go
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/lynx-ui/v0/lynx-ui-cover-sheet_autoheight.webm"
  example="lynx-ui-sheet"
  defaultEntryName="SheetAutoHeight"
  entry="AutoHeight"
  defaultFile="AutoHeight/index.tsx"
  highlight={sheetAutoHeightDemoMeta.highlight}
  schema="{{{url}}}?fullscreen=true&bar_color=0d0d0d&bg_color=0d0d0d"
  webPreviewMode="auto"
/>

## Controlled

Use `show` with `onShowChange` when the open state belongs to the page. This is
the right model when other UI, analytics, or navigation logic must stay in sync
with the sheet.

<Go
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/lynx-ui/v0/lynx-ui-cover-sheet_controlled.webm"
  example="lynx-ui-sheet"
  defaultEntryName="SheetControlled"
  entry="Controlled"
  defaultFile="Controlled/index.tsx"
  highlight={sheetControlledDemoMeta.highlight}
  schema="{{{url}}}?fullscreen=true&bar_color=0d0d0d&bg_color=0d0d0d"
  webPreviewMode="auto"
/>

## Side

Use `side` to choose whether Sheet behaves like a bottom panel, top panel, or
drawer. Prefer `start` and `end` for layouts that should follow RTL, and `left`
or `right` when the physical edge must stay fixed.

<Go
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/lynx-ui/v0/lynx-ui-cover-sheet_directional.webm"
  example="lynx-ui-sheet"
  defaultEntryName="SheetDirectional"
  entry="Directional"
  defaultFile="Directional/index.tsx"
  highlight={sheetDirectionalDemoMeta.highlight}
  schema="{{{url}}}?fullscreen=true&bar_color=0d0d0d&bg_color=0d0d0d"
  webPreviewMode="auto"
/>

## Imperative

Use `SheetRootRef` when a parent needs to command the sheet directly. Methods
such as `open`, `close`, `snapTo`, `expand`, and `collapse` are useful for
toolbar actions and coordinated multi-step flows.

<Go
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/lynx-ui/v0/lynx-ui-cover-sheet_imperative.webm"
  example="lynx-ui-sheet"
  defaultEntryName="SheetImperative"
  entry="Imperative"
  defaultFile="Imperative/index.tsx"
  highlight={sheetImperativeDemoMeta.highlight}
  schema="{{{url}}}?fullscreen=true&bar_color=0d0d0d&bg_color=0d0d0d"
  webPreviewMode="auto"
/>

## Structure

```jsx
<SheetRoot>
  <SheetView>
    <SheetBackdrop />
    <SheetContent>
      <SheetHandle />
    </SheetContent>
  </SheetView>
</SheetRoot>
```

## API
